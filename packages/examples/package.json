{
  "name": "@standard-schema/integration",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "pnpm biome lint ./src",
    "format": "pnpm biome format --write ./src",
    "check": "pnpm biome check ./src",
    "build": "tsup"
  },
  "devDependencies": {
    "arktype": "^2.0.0",
    "typescript": "^5.6.2",
    "valibot": "1.0.0-beta.12",
    "zod": "^3.24.1",
    "@standard-schema/spec": "*"
  }
}
