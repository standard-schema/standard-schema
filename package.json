{
  "name": "standard-schema",
  "description": "A standard interface for TypeScript schema validation libraries",
  "version": "0.0.0",
  "license": "MIT",
  "author": "Colin McDonnell",
  "repository": {
    "type": "git",
    "url": "https://github.com/standard-schema/standard-schema"
  },
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "typescript": "^5.6.2",
    "prettier": "^3.4.2"
  },
  "scripts": {
    "build": "pnpm run --filter=@standard-schema/spec --filter=@standard-schema/utils build"
  }
}
